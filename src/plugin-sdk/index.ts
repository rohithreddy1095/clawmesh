/**
 * ClawMesh plugin-sdk stub.
 *
 * The full plugin SDK (channel plugins, hooks, wizard, etc.) is stripped.
 * Only the JSON store and essential type re-exports remain.
 */
export { JsonStore } from "./json-store.js";
