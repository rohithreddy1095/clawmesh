node_modules/
dist/
.clawmesh-run/
backend.log
.env
.env.local
# contains device credentials — never commit; delete after bootstrap
docs/HANDOFF-2026-07-05-deploy.md
# session artifacts — never commit
pi-session-*.html
session-*.md

# private IP material — never commit
docs/private/
